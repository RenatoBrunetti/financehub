{
  "watch": ["src/**/*.ts", "src/**/*.graphql"],
  "ignore": ["src/**/*.spec.ts"],
  "ext": "ts, graphql",
  "execMap": {
    "ts": "node -r sucrase/register -r tsconfig-paths/register ./src/server.ts"
  }
}
